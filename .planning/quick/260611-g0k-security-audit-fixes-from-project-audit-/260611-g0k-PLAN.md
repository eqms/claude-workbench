---
phase: quick
plan: 260611-g0k
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/pty.rs
  - src/app/clipboard.rs
  - src/update/log.rs
  - Cargo.toml
autonomous: true
requirements:
  - SEC-02-pty-unquoted-path
  - SEC-03-cr-injection
  - SEC-04-dep-pin
  - SEC-05-rustsec-floor
  - SEC-06-log-tempdir

must_haves:
  truths:
    - "insert_path_at_cursor never writes an unquoted raw path to the PTY on shlex::try_quote failure"
    - "copy_selection_to_claude strips \\r from extracted lines before write_input"
    - "tui-textarea git dependency is pinned to rev b6bf812d1f5edab4f311f56d405a47341e9423cf"
    - "tokio floor in Cargo.toml is >= 1.44.2 (RUSTSEC-2025-0023 hygiene)"
    - "update log fallback uses std::env::temp_dir() instead of hardcoded /tmp"
    - "Cargo.toml version is 0.93.0"
    - "cargo build && cargo test && cargo clippy --all-features -- -D warnings && cargo fmt --check all pass"
  artifacts:
    - path: "src/app/pty.rs"
      provides: "Hardened insert_path_at_cursor — early return on quote failure"
    - path: "src/app/clipboard.rs"
      provides: "CR-filtered copy_selection_to_claude"
    - path: "src/update/log.rs"
      provides: "Portable temp_dir() fallback"
    - path: "Cargo.toml"
      provides: "Pinned tui-textarea rev + raised tokio floor + v0.93.0"
  key_links:
    - from: "src/app/pty.rs"
      to: "shlex::try_quote"
      via: "error arm must return early — must not call write_input"
      pattern: "unwrap_or_else"
    - from: "src/app/clipboard.rs"
      to: "pty.write_input"
      via: "lines must have \\r stripped before join and write"
      pattern: "replace.*\\\\r"
---

<objective>
Apply five security/hygiene fixes identified in the /project-audit (eager-inventing-quilt.md).
The HIGH finding (self-update signature verification) is explicitly OUT OF SCOPE — handled
separately.

Purpose: Harden the PTY path-insert fallback, prevent CR-injection to Claude PTY, pin
tui-textarea supply-chain ref, raise tokio floor above RUSTSEC-2025-0023 range, and replace the
hardcoded /tmp log fallback with std::env::temp_dir().

Output:
- src/app/pty.rs — early return on shlex failure, never raw write
- src/app/clipboard.rs — \\r stripped from lines before write_input
- src/update/log.rs — portable temp_dir() fallback
- Cargo.toml — rev pin + tokio 1.44.2 floor + version 0.93.0
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/picard/gitbase/workbench/.planning/STATE.md
@/Users/picard/gitbase/workbench/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden PTY path insert + CR filter in clipboard</name>
  <files>src/app/pty.rs, src/app/clipboard.rs</files>
  <action>
**src/app/pty.rs — insert_path_at_cursor (current ~line 413-426):**

Replace the `unwrap_or_else(|_| path_str.into_owned())` fallback with an early return on failure,
matching the pattern already used by `quote_path_for_cd` (~line 435). The corrected function body:

```
// shlex::try_quote fails only on NUL-byte paths (invalid on Unix/Windows).
// On failure, log a debug note and return without writing anything to the PTY.
// Never write a raw, unquoted path — it could execute shell metacharacters.
let escaped = match shlex::try_quote(&path_str) {
    Ok(c) => c.into_owned(),
    Err(_) => {
        // Path contains a NUL byte — reject silently (unreachable on sane FS).
        return;
    }
};
let _ = pty.write_input(escaped.as_bytes());
```

Remove the comment about "only fails on NUL bytes" from the `unwrap_or_else` line and move it to the
match arm. Keep the surrounding `if let Some(pty) = ...` guard intact. Do NOT add a newline after
`escaped` — the existing behaviour is insert-only (no newline).

---

**src/app/clipboard.rs — copy_selection_to_claude:**

The `formatted_lines` Vec comes from `extract_lines()` which returns raw VT100 screen rows — these
can contain trailing `\r` from CRLF line endings in PTY output. Before the `format!()` call that
builds the markdown code block, strip `\r` from every line:

```
let formatted_lines: Vec<String> = formatted_lines
    .into_iter()
    .map(|l| l.replace('\r', ""))
    .collect();
```

Insert this normalisation immediately after the `(formatted_lines, syntax_hint)` binding is
resolved from the if/else block and before the `let syntax = ...` line. The variable name
`formatted_lines` is rebound — shadow the existing binding.

Do not alter the `filter_lines()` call, `FilterOptions`, or any other logic in this function.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/workbench && cargo build 2>&1 | tail -5 && cargo test 2>&1 | tail -10 && cargo fmt --check 2>&1</automated>
  </verify>
  <done>
    - cargo build succeeds with no errors
    - insert_path_at_cursor contains no unwrap_or_else on the shlex::try_quote result
    - copy_selection_to_claude has a .replace('\r', "") normalisation before write_input
    - cargo test passes (139 tests or more)
  </done>
</task>

<task type="auto">
  <name>Task 2: Cargo.toml — pin tui-textarea rev + raise tokio floor + version bump</name>
  <files>Cargo.toml</files>
  <action>
Make three edits to Cargo.toml:

**1. tui-textarea rev pin (line 14):**
The current entry is:
  tui-textarea = { git = "https://github.com/0xferrous/tui-textarea.git", branch = "update-ratatui" }

Replace with:
  tui-textarea = { git = "https://github.com/0xferrous/tui-textarea.git", branch = "update-ratatui", rev = "b6bf812d1f5edab4f311f56d405a47341e9423cf" }

This rev is what Cargo.lock already resolves (confirmed:
`git+https://github.com/0xferrous/tui-textarea.git?branch=update-ratatui#b6bf812d1f5edab4f311f56d405a47341e9423cf`).
The pin is a no-op for the current build but prevents silent drift on future `cargo update`.

**2. tokio floor (line 9):**
Change:
  tokio = { version = "1.44.0", features = ["rt-multi-thread"] }
To:
  tokio = { version = "1.44.2", features = ["rt-multi-thread"] }

Rationale: RUSTSEC-2025-0023 affects 1.44.0–1.44.1. Lock already resolves 1.49.0 so this is a
floor hygiene change only — no actual dependency version changes.

**3. Version bump (line 3):**
Change:
  version = "0.92.0"
To:
  version = "0.93.0"

This is a [FIX] security release per project convention. Do NOT update any date field in Cargo.toml
(there is none). The version header in CLAUDE.md is documentation, not a source file — leave it.

After editing, run `cargo build` to confirm Cargo.lock is NOT regenerated with a different tui-textarea commit hash. If the hash in Cargo.lock changes from `b6bf812d1f5edab4f311f56d405a47341e9423cf`, STOP and report — do not proceed to commit.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/workbench && grep 'version = "0.93.0"' Cargo.toml && grep 'rev = "b6bf812d1f5edab4f311f56d405a47341e9423cf"' Cargo.toml && grep '1.44.2' Cargo.toml && grep 'b6bf812d1f5edab4f311f56d405a47341e9423cf' Cargo.lock && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Cargo.toml version = "0.93.0"
    - tui-textarea entry contains rev = "b6bf812d1f5edab4f311f56d405a47341e9423cf"
    - tokio floor is 1.44.2
    - Cargo.lock still resolves tui-textarea to the same b6bf812 commit
    - cargo build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix update log /tmp fallback + final verification + commit</name>
  <files>src/update/log.rs</files>
  <action>
**src/update/log.rs — log_file_path():**

Current code:
```
pub fn log_file_path() -> std::path::PathBuf {
    dirs::cache_dir()
        .unwrap_or_else(|| std::path::PathBuf::from("/tmp"))
        .join("claude-workbench")
        .join("update.log")
}
```

Replace the hardcoded `/tmp` fallback with `std::env::temp_dir()`:
```
pub fn log_file_path() -> std::path::PathBuf {
    dirs::cache_dir()
        .unwrap_or_else(std::env::temp_dir)
        .join("claude-workbench")
        .join("update.log")
}
```

`std::env::temp_dir()` honours `$TMPDIR` on macOS/Unix and `GetTempPath` on Windows — portable and
not a predictable static path. No other changes to this file.

---

**Final verification — run all gates:**

After the edit, run the full verification suite:
  cargo build
  cargo test
  cargo clippy --all-features -- -D warnings
  cargo fmt --check

Fix any clippy warnings introduced by this task's edits before committing. Pre-existing clippy
failures (the 16 known collapsible_if/useless_vec errors noted in STATE.md) are out of scope — do
not fix them here.

---

**Commit:**

Stage and commit with:
  git add src/app/pty.rs src/app/clipboard.rs src/update/log.rs Cargo.toml Cargo.lock
  git commit -m "[FIX] v0.93.0: security audit fixes — PTY path quoting, CR filter, dep pins, log tempdir"

Do NOT push. Do NOT touch any self-update signature code.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/workbench && cargo build 2>&1 | tail -5 && cargo test 2>&1 | tail -5 && cargo clippy --all-features -- -D warnings 2>&1 | grep -v "^warning\[" | tail -20 && cargo fmt --check 2>&1 && git log --oneline -3</automated>
  </verify>
  <done>
    - log.rs fallback uses std::env::temp_dir (no /tmp literal)
    - cargo build clean
    - cargo test passes
    - cargo clippy --all-features -- -D warnings produces no errors from files touched by this plan
    - cargo fmt --check clean
    - git log shows [FIX] v0.93.0 commit at HEAD
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem path → PTY stdin | File paths from the browser pane enter the shell; quoting failure must never pass raw bytes |
| PTY screen buffer → Claude PTY stdin | Extracted terminal lines re-enter Claude as input; CRLF must not trigger premature submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g0k-01 | Tampering | insert_path_at_cursor / PTY stdin | mitigate | Remove unwrap_or_else fallback; return early on shlex failure — raw path never written |
| T-g0k-02 | Tampering | copy_selection_to_claude / PTY stdin | mitigate | Strip \\r before write_input; prevents premature command submission via CR injection |
| T-g0k-03 | Tampering | tui-textarea git dep (supply chain) | mitigate | Pin rev= to the commit already in Cargo.lock; future `cargo update` cannot silently drift |
| T-g0k-04 | Denial of Service | tokio 1.44.0 (RUSTSEC-2025-0023) | mitigate | Raise floor to 1.44.2; lock already at 1.49.0 — floor is hygiene |
| T-g0k-05 | Tampering | /tmp log path (symlink) | mitigate | Replace with std::env::temp_dir() — honours $TMPDIR, not a predictable static path |
| T-g0k-SC | Tampering | npm/pip/cargo installs | accept | Rust crate changes are rev-pinned or floor-adjustments only; no new crates introduced |
</threat_model>

<verification>
Full suite must pass before commit:

```
cargo build
cargo test
cargo clippy --all-features -- -D warnings
cargo fmt --check
```

Manual smoke checks (optional, post-commit):
- Insert a file with spaces in its name via the file browser path-insert feature — verify it arrives quoted in the PTY
- Copy terminal lines containing \\r\\n output to Claude (e.g. from a Windows-style command) — verify no premature Enter is submitted
- `./target/release/claude-workbench --check-update` — verify update check still works (log path resolves correctly)
</verification>

<success_criteria>
- All five audit findings (SEC-02 through SEC-06) resolved with targeted edits — no scope creep
- Version bumped to 0.93.0 per [FIX] release convention
- Cargo.lock tui-textarea commit hash unchanged after Cargo.toml rev pin added
- Self-update signature code (src/update/install.rs) untouched
- Full CI gate (build + test + clippy + fmt) passes locally before commit
</success_criteria>

<output>
Create `.planning/quick/260611-g0k-security-audit-fixes-from-project-audit-/260611-g0k-SUMMARY.md` when done
</output>
